---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Rucha Khanolkar">
  <div style="background-color: #FDFBD4; min-height: 100vh; display: flex; align-items: center; justify-content: center;">
    <!-- Hero Section -->
    <section class="w-full py-8">
    <div class="max-w-5xl mx-auto px-6 sm:px-8 lg:px-12">
      <div class="flex flex-col lg:flex-row lg:justify-between lg:items-center gap-12">
        <!-- Text Content - Top Left -->
        <div class="text-left flex-1">
          <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-6 leading-tight" style="color: #2E6F40;">
            Hi, I'm Rucha!
          </h1>
          <p class="text-lg sm:text-xl text-gray-700 font-light max-w-2xl mb-6">
            I'm a product manager who loves creating user-centered solutions. Currently pursuing my MBA at NYU Stern where I'm building on my design roots to shape thoughtful, impactful products.
          </p>
          <p class="text-sm text-gray-600 mb-12">
            📚 <a href="https://www.goodreads.com/user/show/125144933-rucha" target="_blank" rel="noopener noreferrer" class="hover:text-gray-800 transition-colors">What I'm reading right now.</a>
          </p>
          
          <!-- Navigation Links -->
          <div class="navigation-links flex flex-wrap gap-3 mt-8">
            <button onclick="openSidePanel('work')" class="px-4 py-2 rounded-full transition-colors text-sm font-medium cursor-pointer" style="background-color: #2E6F40; color: #FDFBD4;">
              Work
            </button>
            <button onclick="openSidePanel('ceramics')" class="px-4 py-2 rounded-full transition-colors text-sm font-medium cursor-pointer" style="background-color: #2E6F40; color: #FDFBD4;">
              Ceramics
            </button>
            <button onclick="openSidePanel('archived')" class="px-4 py-2 rounded-full transition-colors text-sm font-medium cursor-pointer" style="background-color: #2E6F40; color: #FDFBD4;">
              Older Stuff
            </button>
            <button onclick="openSidePanel('aboutme')" class="px-4 py-2 rounded-full transition-colors text-sm font-medium cursor-pointer" style="background-color: #2E6F40; color: #FDFBD4;">
              About Me
            </button>
          </div>
        </div>
        
        <!-- Illustration - Right Side -->
        <div class="hidden lg:flex justify-center lg:justify-end flex-shrink-0">
          <div class="w-72 h-80 max-w-full">
            <img 
              src="/rucha.png" 
              alt="Illustrated portrait of Rucha" 
              class="w-full h-full object-contain"
            />
          </div>
        </div>
      </div>
    </div>
    </section>
  </div>

  <!-- Side Panel Overlay -->
  <div id="sidePanel" class="fixed inset-0 z-50 hidden">
    <!-- Backdrop -->
    <div class="absolute inset-0 bg-black bg-opacity-50" onclick="closeSidePanel()"></div>
    
    <!-- Side Panel -->
    <div class="absolute right-0 top-0 w-full md:w-1/2 bg-white shadow-2xl transform translate-x-full transition-transform duration-300 ease-in-out max-h-screen overflow-hidden" id="sidePanelContent">
      <!-- Header -->
      <div class="flex items-center justify-between p-4 md:p-6 border-b border-gray-200 flex-shrink-0">
        <h2 id="sidePanelTitle" class="text-lg md:text-xl font-light text-gray-900"></h2>
        <button onclick="closeSidePanel()" class="p-3 md:p-2 hover:bg-gray-100 rounded-full transition-colors">
          <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
      
      <!-- Content -->
      <div class="p-4 md:p-6 overflow-y-auto max-h-[calc(100vh-80px)] pb-32 md:pb-96" id="sidePanelBody">
        <!-- Dynamic content will be loaded here -->
      </div>
    </div>
  </div>
</Layout>


